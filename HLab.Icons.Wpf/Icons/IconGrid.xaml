<UserControl x:Class="HLab.Icons.Wpf.Icons.IconGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:icons="clr-namespace:HLab.Icons.Wpf.Icons" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <ContentControl x:Name="MainIcon"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
                <ContentControl x:Name="TopLeftIcon" Grid.Row="0" Grid.Column="0"/>
                <ContentControl x:Name="TopRightIcon" Grid.Row="0" Grid.Column="1"/>
                <ContentControl x:Name="BottomLetIcon" Grid.Row="1" Grid.Column="0"/>
                <ContentControl x:Name="BottomRightIcon" Grid.Row="1" Grid.Column="1"/>
            </Grid>
    </Grid>
</UserControl>
