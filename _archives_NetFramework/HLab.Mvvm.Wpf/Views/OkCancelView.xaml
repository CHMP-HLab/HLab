<UserControl x:Class="HLab.Mvvm.Wpf.Views.OkCancelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HLab.Mvvm.Wpf.Views"
             xmlns:wpf="clr-namespace:HLab.Mvvm.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <UserControl.Resources>
        <wpf:LocalizationConverter x:Key="LocalizationConverter"/>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button 
            Margin="5" 
            Width="{Binding Width, ElementName=CancelButton}"
            Content="{Binding Localization, Converter=LocalizationConverter, ConverterParameter=Ok}"
            Command="{Binding OkCommand}"
            />
        <Button 
            x:Name="CancelButton" 
            Grid.Column="1" 
            Margin="5"
            Content="{Binding Localization, Converter=LocalizationConverter, ConverterParameter=Cancel}"
            Command="{Binding CancelCommand}"
            />
    </Grid>
</UserControl>
